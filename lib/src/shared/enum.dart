enum Routes {
  home, intro, voiceOptions, connect, import, login, deployAgent, dashboard, requestPermissions, profile, reDeploAgent, editProfile, payment, paymentSuccess, agentChat, agentCall, agentVoice,
}